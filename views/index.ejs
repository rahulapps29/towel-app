<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title>Where is My Towel?</title>
  </head>
  <body>
    <div class="container">
      <h1>Where is my towel?</h1>
      <form action="/submit" method="POST">
        <label>
          <input type="radio" name="floor" value="ground floor" <%=
          selectedFloor === 'ground floor' ? 'checked' : '' %> required> Ground
          Floor
        </label>
        <br />
        <label>
          <input type="radio" name="floor" value="first floor" <%= selectedFloor
          === 'first floor' ? 'checked' : '' %> required> First Floor
        </label>
        <br />
        <label>
          <input type="radio" name="floor" value="second floor" <%=
          selectedFloor === 'second floor' ? 'checked' : '' %> required> Second
          Floor
        </label>
        <br /><br />
        <button type="submit">Submit</button>
      </form>
    </div>
  </body>
</html>
